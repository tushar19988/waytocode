package com.example.waytocode.ViewHolder

import android.view.View
import androidx.recyclerview.widget.RecyclerView

class ServiceViewHolder(view: View): RecyclerView.ViewHolder(view)

   /* init {

        itemView.setOnClickListener {

            listener.onItemClick(adapterPosition)
        }
    }

    private fun ViewHolder(itemView: View) {

        itemView.setOnClickListener(this)
        var textView = itemView.findViewById<TextView>(R.id.servicename)
    }

    override fun onClick(v: View?) {
        Toast.makeText(itemView.getContext(), "position = " + getLayoutPosition(), Toast.LENGTH_SHORT).show();

        //go through each item if you have few items within recycler view
        if(getLayoutPosition()==0){
            //Do whatever you want here
            val intent = Intent(itemView.context, WebdesigningActivity::class.java)
            startActivity(this)


        }else if(getLayoutPosition()==1){
            //Do whatever you want here

        }else if(getLayoutPosition()==2){

        }else if(getLayoutPosition()==3){

        }else if(getLayoutPosition()==4){

        }else if(getLayoutPosition()==5){

        }
    }



*/





